## Java Lab 1 – Composite Book Model

Repository now contains a minimal Java port of Lab 1. The focus is on the book model (Book/Section/Paragraph/Image) using a Composite structure.

### Running
```
javac *.java
java Main
```

### Notes
- All previous C# files were removed per requirement.
- `.class` artifacts are not tracked; run `javac` locally when needed.

